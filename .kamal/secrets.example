# Copy to .kamal/secrets and chmod 600. Never commit live credentials.
# The shared loopback registry has no authentication; Kamal requires a value.
KAMAL_REGISTRY_PASSWORD=local-registry
API_TOKENS=replace-with-existing-renderer-secret
